import { StyleSheet } from 'react-native'

import { ApplicationStyles } from '../../../shared/themes'

export default StyleSheet.create({
  ...ApplicationStyles.screen,
  container: {
    flex: 1,
    padding: 20
  },
  label: {
    fontWeight: 'bold'
  },
  <%_ if (entityContainsImageBlob) { _%>
  imageBlob: {
    width: 100,
    height: 50,
    resizeMode:'contain',
    borderWidth: 1,
    borderColor: 'lightgrey'
  },
  <%_ } _%>
})
